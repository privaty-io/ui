/**
 * Testing barrel for `@privaty/ui-forms/testing` — Kit-faithful fakes for
 * consumers' component tests. Deliberately a SEPARATE barrel: test doubles
 * never mix into the app-facing root export.
 */
export * from "./fakes.svelte";
