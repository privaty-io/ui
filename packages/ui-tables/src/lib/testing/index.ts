/**
 * Testing barrel for `@privaty/ui-tables/testing` — the table-shaped
 * remote-form fakes for consumers' component tests. Separate from the
 * root barrel on purpose: test doubles never mix into the app surface.
 */
export * from "./fakes.svelte";
